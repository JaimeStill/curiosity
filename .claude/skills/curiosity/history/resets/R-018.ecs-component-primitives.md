# R-018 — ECS component primitives — 2026-05-14

**Scope**: engine (with a project-scoped item for the
`execution.md` revision).

**Trigger**: Standing reset trigger — the post-session planning
discussion at the end of the `ecs-component-primitives` session
(`execution.md` step 3). Carried forward from R-017's named
next-session focus (originally surfaced in R-016).

**Integrated**:

- **`engine/ecs/component/` package** — landed under
  `engine/ecs/component/component.go`,
  `engine/ecs/component/registry.go`, and
  `engine/ecs/component/signature.go`, with colocated black-box
  tests in `engine/ecs/component/component_test.go` per D-032.
  Surface honors `design/engine/components/ecs.md`'s prior spec:
  `component.ID` (uint16; valid IDs start at 1; `InvalidID = 0`
  is the sentinel), `component.Value` (type-erased
  `(ID, unsafe.Pointer)` inner-tier carrier per conventions §2),
  process-wide `IDFor[T]() ID` / `TypeOf(ID) reflect.Type`
  registry (lock-free per D-024; revisit pointer in registry
  godoc), `MaxCID = 2048`, `Signature [MaxCID/64]uint64` with
  `Set`/`Has`/`Contains`/`SignatureOf`. Godoc on every exported
  symbol; inner-tier divergence comments at `Value` and at the
  registry vars; one user-authored block comment in
  `signature.go` retained as embedded learning material for the
  bitset arithmetic.
- **`design/engine/components/ecs.md`** —
  `engine/ecs/component/` sub-section absorbed: the four-bullet
  primitives list and the "Designed fresh against the
  no-graduation rule…" historical sentence were removed (the
  primitives are now expressed by source and godoc). The
  sub-section was reshaped to mirror `engine/ecs/entity/`'s
  post-absorption form — source-pointer intro plus
  forward-looking close — and a MaxCID sizing paragraph was
  added carrying the WHY source cannot express: alignment
  between `Signature` and archetype's `columnFor`, rejection of
  the "carry uint64 forward" plan-exit alternative, and the
  trigger condition (D-027's archetype-table memory growth
  question) for revisiting 2048.
- **`.claude/behavior/execution.md`** — closeout step 3 bullet
  list revised. `(d) any experiment removals whose job has
  ended` was dropped; `(e) the next session's focus and type`
  renumbered to `(d)`. Resolves a contradiction with SKILL.md's
  Reset Protocol section ("Experiments are not culled — once
  complete, they remain as historical artifacts (D-033)").

Verification: `go vet ./...` clean across the engine module;
`go test ./...` passes across all four packages (root engine,
ecs/entity, ecs/component, lifecycle); read-through of modified
`ecs.md` and `execution.md` confirms internal consistency;
cross-references between SKILL.md's Reset Protocol and
execution.md step 3 now agree.

**Promoted**: None.

**Culled**: None.

**Retained**:

- All five `concepts/engine/` files from R-017's Retained list
  (`outer-tier-contract.md`, `scheduler.md`, `render-thread.md`,
  `rendering-primitives.md`, `animation-approaches.md`).
  Untouched this session.
- `experiments/ecs-storage-comparison/` per D-033. The
  component-portion findings have now informed engine source
  (D-033's success path); the experiment stays as historical
  artifact.

**Decisions promoted**: None. The MaxCID sizing rationale was
captured in engine design rather than logged as a decision —
decisions are history-level and process-oriented; design is the
forward-looking codified reference for engine facts. Sizing
facts about an inner-tier primitive belong in design.

**Workflow observation this session**:

- **Source-code boundary slip in checkpoint 1.** The Write tool
  was used to author the first `.go` file directly instead of
  issuing the scene-setting prelude + source-only-fenced-block
  checkpoint shape per `.claude/behavior/source-code.md`. The
  user caught it mid-call; recovery was loading `source-code.md`
  and reissuing the checkpoint in the correct shape. Pattern:
  when development phase begins, load `source-code.md` before
  the first checkpoint output.
- **Over-verification cadence.** The session plan baked in
  "each checkpoint ends with `go vet ./...`" — vet and build
  were run after every checkpoint and once mid-typing. User
  corrected: vet and tests run once at the end of the
  development phase, not per checkpoint. Plan-template
  implication: future session plans should not include
  per-checkpoint vet runs.

**Next session focus**: **Context session** — the first
*decision compaction pass* and *reset cull pass* against the
current history logs, per R-017's "Follow-up after that session"
clause. Accumulated state after this entry lands: 34 decisions
(D-001..D-034) and 18 resets (R-001..R-018). Treats SKILL.md's
Compaction Operations conventions (codified in R-017) as a
hypothesis to validate; expect refinements to the procedure to
surface as it runs in practice.
