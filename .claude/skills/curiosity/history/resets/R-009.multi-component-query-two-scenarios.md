# R-009 — multi-component query workload across three active backends, run as two isolated scenarios — 2026-05-07

**Scope**: engine

**Trigger**: Carryout of R-008's "Next session focus" — implement multi-component query workload across archetype + sparsesetslice + sparsesetgroup at 1k/10k/100k. Mid-session expansion to two isolated scenarios (`multi_full` / `multi_partial`) after the user surfaced wanting holistic profile coverage of sparsesetgroup's fast-path/fallback split rather than a single query shape.

**Integrated**:
- `experiments/ecs-storage-comparison/workload/components.go` — `Health{Current, Max int32}` and `Tag{V byte}` types added alongside existing `Position`, `Velocity`. `Tag` added specifically to fragment archetype's matching set into multiple matching archetypes per scenario; without it, scenario 1's query of `{P,V,H}` would match only one archetype and the multi-archetype-walk cost the workload exists to surface would be invisible.
- `experiments/ecs-storage-comparison/workload/multicomponent.go` — `MultiGroups()` declaring single owning group `{P,V,H}` (shared across both scenarios); `MultiComponentSetup` spawning N entities across six composition classes cycled by `i % 6` (`{P,V,H,Tag}`, `{P,V,H}`, `{P,V,Tag}`, `{P,V}`, `{P,H,Tag}`, `{V,Tag}`); `MultiFullTick` querying `{P,V,H}` with `pos += vel` and `health.Current -= 1` (the health write keeps the third-component `Get` engaged against compiler dead-code-elimination); `MultiPartialTick` querying `{P,V}` with `pos += vel`.
- `experiments/ecs-storage-comparison/main.go` — `selectWorkload` extended with `multi_full` and `multi_partial` cases; `selectWorkloadGroups` extended with shared `multi_full, multi_partial` case returning `MultiGroups()`. `newBackend` unchanged.
- `experiments/ecs-storage-comparison/README.md` — *Approach > Workloads > 2* reframed for two-scenario shape with per-scenario behavior described per backend; package-layout `workload/` line updated to enumerate the new symbols; *Runtime > How to run* `-workload` valid values updated. New dated Finding entry (2026-05-07) covering both scenarios: per-N cost table (3 backends × 3 scales × 2 scenarios), per-iterated-row cost table cross-referenced against R-008's iteration baseline at 100k, distribution tables at 100k for each scenario, scenario-specific *Why* narratives explaining per-row probe behavior and per-archetype-walk amortization, allocation parity, heap profile, unified-iterator-tax caveat, *Implication for the engine decision* paragraph carrying the D-022 evaluation-criterion framing applied to today's data, status.
- `experiments/ecs-storage-comparison/workload/README.md` — new functional-spec README following D-021's discipline. Covers the package's role and component types; describes the iteration, multi_full, and multi_partial workloads with composition diagrams, spawn patterns, and per-backend behavior under each one. Group-declarations cross-cut documents how each workload wires `*Groups()` for `sparsesetgroup`. Authored as part of closeout when the user surfaced the workload package's documentation gap parallel to R-008's per-backend READMEs.

**Promoted**: None. ECS storage strategy (`concepts/engine/ecs-storage.md`) remains concept-tier — multi-component data extends what's known but does not settle the storage decision. Three workloads remain (structural churn, attach/detach churn, mixed) plus six interface methods stubbed across each of the three active backends.

**Culled**: None this session. (sparsesetmap was culled forward in R-007; that's already-recorded state.)

**Retained**:
- `experiments/ecs-storage-comparison/` — experiment in flight. Six interface methods (Despawn / Attach / Detach / Read / Write / ApplyDeferred) × three active backends pending; three remaining workloads pending.
- `concepts/engine/ecs-storage.md` — still load-bearing. The session's data narrows the field but does not settle it.
- `concepts/engine/entity-allocator.md` — untouched, still load-bearing.
- `concepts/engine/scheduler.md`, `render-thread.md`, `outer-tier-contract.md`, `rendering-primitives.md`, `animation-approaches.md` — engine-side concept stubs untouched.
- Five game-side concept stubs from R-004 — untouched.

**Decisions promoted**:
- D-022 — Architectural alternatives weighed across performance, complexity, and ergonomics jointly (project scope). Surfaced mid-session when the user articulated their evaluation framework in response to the analysis of sparsesetgroup's leadership flip between `multi_full` and `multi_partial`. Generalized from the immediate storage-decision question into a project-scope criterion applicable to future architectural choices. The Finding entry's *Implication for the engine decision* paragraph applies the criterion to today's data, naming archetype's strengthening standing on the joint axis after this workload.

**Next session focus**: Experiment-type session continuing on `experiment-ecs-storage-comparison`. Two deliverables sized to the session's expected headroom:

1. **Implement `Despawn` and `ApplyDeferred` across all three active backends** (archetype, sparsesetslice, sparsesetgroup) — six method implementations total, plus the per-backend command-buffer design they imply. The deferred-mutation surface enters scope for the first time. Design questions to settle in the development phase: command-buffer shape per backend; for sparsesetslice and sparsesetgroup, the entity-to-column-membership lookup question (does each backend track which columns own each entity, or does Despawn scan all columns?); for sparsesetgroup, lockstep-on-Despawn coordination across group columns when the despawned entity sits in the owned prefix.

2. **Build out the structural-churn workload integration** — third row of the workload × backend matrix per *Approach > Workloads > 3*. Spawn/despawn at varying rates per frame; design choices (steady-state vs. churn-and-grow, per-frame churn rate range) to settle in the development phase. Measurements at 1k/10k/100k across the three active backends produce the third Finding entry. The new workload entry is added to `workload/README.md` alongside its source — composition diagram, per-tick churn pattern, per-backend behavior — keeping that README current with what's been developed.

Headroom across the last several sessions has been solid; this scope is plausible. The structural-mutation design depth is the unknown — if the deferred-mutation surface or the column-membership lookup question expands beyond expectation, the workload integration carries into a subsequent session, with `Despawn` + `ApplyDeferred` prioritized.

**Workflow observations this session**:
- **Mid-session synthesis interrupt was load-bearing.** The user paused the README writing to ask "describe the results to me" before the Finding entry codified the analysis. The synthesis surfaced the trade-off framing that became D-022 — folded into the Finding entry's *Implication for the engine decision* paragraph in the same checkpoint, not a separate revision pass. Pattern: when measurement is finished but interpretation is still in flight, ask for or accept synthesis-before-codification interrupts; they prevent the durable record from solidifying around an incomplete framing.
- **Two-scenario framing emerged from clarification dialogue, not from initial plan.** R-008's "Next session focus" used phrasing that admitted two readings (fast-path vs. fallback-path measurement). The first `AskUserQuestion` got "I don't have context"; elaborating with concrete data-flow tables (per-backend behavior under each option, mechanism plus measurement target) led to "test each path in isolation as separate workload scenarios" as the user's holistic preference. Pattern: when a clarification question gets deferred for lack of context, the next response supplies the mechanism, not just the choice — the mechanism is what makes the choice tractable.
- **Source-code authorship pattern continued cleanly across four checkpoints.** Only modification the user made to typed source was reformatting case 0's variadic `Spawn` call onto multiple lines for readability — no semantic change. The "I scene-set, you type" rhythm is now predictable enough that scene-setting can lead with substance rather than with format reminders.
- **Single Finding entry covered both scenarios rather than two parallel entries.** The two scenarios share setup, share group declaration, and share the engine-decision framing; splitting them would have duplicated context across two Finding entries. Format choice for future workloads with multiple scenarios: one Finding entry per workload, with sub-scenarios as parallel sections within.
