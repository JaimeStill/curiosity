# R-017 — compaction protocol — 2026-05-13

**Scope**: project

**Trigger**: Standing reset trigger — the post-session planning discussion at the end of the compaction-protocol session (`execution.md` step 3). This session was the "Additional planning" thread carried alongside the R-016 next-session focus. The named `ecs-component-primitives` session immediately follows on a separate branch within the same calendar period.

**Integrated**:

- **`SKILL.md`** — new top-level **Compaction Operations** section between Reset Protocol and "When This Skill Activates," documenting the decision compaction pass and reset cull pass. Triage buckets and outputs (a reset entry with `Trigger: Decision compaction pass` for the decision pass; a one-line maintenance note for the cull pass) defined here. The Reset cull pass triage is relevance-based rather than recency-based — the most recent reset is usually retained because its Next-session-focus is typically still load-bearing, but that is an emergent property of the relevance test, not an axiom.
- **`SKILL.md`** — *Compaction exception* pointer added to the Decisions Log section after the append-only paragraph; *Cull exception* pointer added at the end of the Reset Protocol section. Both forward to Compaction Operations.
- **`.claude/behavior/execution.md`** — new **On-demand maintenance** section at the bottom, noting that compaction and cull passes are user-invoked and distinct from session closeout. References `SKILL.md` as the authoritative protocol location.

Verification: read-through of both modified files in context; cross-references (Compaction exception → Compaction Operations; Cull exception → Compaction Operations; execution.md → SKILL.md) all resolve.

**Promoted**: None.

**Culled**: None.

**Retained**:

- All engine and game concepts from R-016's Retained list — `concepts/engine/outer-tier-contract.md`, `concepts/engine/scheduler.md`, `concepts/engine/render-thread.md`, `concepts/engine/rendering-primitives.md`, `concepts/engine/animation-approaches.md`, and the five game-side concept stubs from R-004. Untouched this session.
- `experiments/ecs-storage-comparison/` — retained per D-033.
- The pending `ecs-component-primitives` next-session focus from R-016 — preserved as this session's named follow-up (option A from the session-mid alignment). That work moves to its own branch.

**Decisions promoted**:

- **D-034 — Compaction operations for the history logs** (project scope). Codifies the two on-demand maintenance passes and the choices behind their shape — rolling archive, renumbering, named exception over dropping append-only, relevance-based retention rather than recency.

**Workflow observation this session**:

- **Session-start misread.** The "Additional planning" framing in the user prompt was read as a full session topic, displacing the R-016 next-session focus. The user surfaced this mid-edit ("Are we not handling the component foundation infrastructure this session?"). Pattern: when a session prompt names new work alongside a prior reset's next-session focus, the relationship should be surfaced explicitly at session-start orientation rather than one being implicitly chosen.

**Next session focus**: **Development session** — `ecs-component-primitives` (carried forward from R-016 unchanged). The component primitives in isolation under `engine/ecs/component/`:

- `component.ID` per D-030 §6 / design `ecs.md` — unsigned 16-bit, sized for archetype's flat-array column index. `component.InvalidID = 0`; valid IDs start at 1.
- `component.Value` — type-erased `(CID, unsafe.Pointer)` internal mechanism per `ecs.md` and conventions §2 (inner-tier divergence justified by hot-path constraints; `unsafe.Pointer` is contained to this carrier).
- Type → `component.ID` registry — `IDFor[T]() component.ID` cached lookup from `reflect.Type`. Registration on first reference.
- `component.Signature` — bitset over component IDs aligned with MaxCID; methods per the experiment's surface (`Set`, `Has`, `Contains`, `SignatureOf`).
- Godoc and colocated black-box tests per D-032.

Same surface-only posture as R-016's entity primitives session: the package lands in isolation, no wiring into `Engine`, no integration with `entity`. The no-graduation rule applies — designed fresh against the experiment's `component/` as a guide; the experiment stays as historical reference under D-033.

**Follow-up after that session**: a **context session** exercising the new compaction conventions against the current history logs. First decision compaction pass and first reset cull pass against the accumulated 34 decisions and (after R-018) ~18 resets. Treats this session's conventions as a hypothesis to validate; expect refinements to surface as the procedures run in practice.
