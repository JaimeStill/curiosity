# R-008 — sparsesetgroup at iteration-baseline fidelity; per-backend functional-spec READMEs across all four backends — 2026-05-06

**Scope**: engine

**Trigger**: Carryout of R-007's "Next session focus" — implement `experiments/ecs-storage-comparison/sparsesetgroup/` to iteration-baseline fidelity, run iteration baseline at 1k/10k/100k, complete the four-cell iteration table. Mid-session expansion to author per-backend functional-spec READMEs across all four backends after the user surfaced the documentation-tier gap.

**Integrated**:
- `experiments/ecs-storage-comparison/sparsesetgroup/` — `sparsesetgroup.go` with `column` type plus `appendValue` / `ensureSparseCapacity` / `swapDenseRows` helpers, `Storage` with var assertion, six stubbed Storage methods, `New(groups [][]ComponentID)` declaring owning groups at construction, `Spawn` with two-phase append-then-lockstep-swap, `Query` with set-matching for fast path and slice-style fallback, `getOrCreateColumn`. `group.go` with `group` type, `coveredBy` (one-direction containment for Spawn), `matches` (set equality for Query). `iterator.go` with `queryRef`, dual-mode `iterator` carrying `group *group` mode flag, `Next` branching on mode (fast-path: walks `[0, g.size)`, writes index = row; fallback: identical to slice variant), `Entity` and `Get` unified across both modes.
- `experiments/ecs-storage-comparison/main.go` — `newBackend` signature gained `groups [][]storage.ComponentID` parameter, sparsesetgroup case + import added; new `selectWorkloadGroups` dispatch parallel to existing `selectWorkload`.
- `experiments/ecs-storage-comparison/workload/iteration.go` — `IterationGroups()` returning the iteration-baseline owning group `{Position, Velocity}`.
- `experiments/ecs-storage-comparison/README.md` — Backends section 3 implementation paragraph (declaration-at-construction, lockstep-on-Spawn, fast-path/fallback iteration); package-layout sparsesetgroup line; `-backend` valid values updated; new dated Finding entry with four-cell iteration table at 1k/10k/100k, distribution stats at 100k, headline (sparsesetgroup ~10–13% faster than archetype and sparsesetslice), explanation (lockstep invariant skips per-row sparse-side load), allocation parity, heap profile, unified-iterator fast-path tax caveat, implication for engine decision, status.
- Per-backend functional-spec READMEs at `experiments/ecs-storage-comparison/{archetype,sparsesetslice,sparsesetmap,sparsesetgroup}/README.md`. Each describes its backend's data model, algorithm shape, and structural behaviors using concrete-entity diagrams and walked-through scenarios. The format is illustrative (data-layout diagrams, scenario-driven operation traces) rather than specification (field bullet lists). sparsesetmap's README is short and references the slice variant for shared structure, focusing on the map sparse-mapping difference and the cull-from-forward-implementation status. archetype's README walks through the multiple-archetype case to motivate the strategy distinct from sparse-set.

**Promoted**: None. ECS storage strategy (`concepts/engine/ecs-storage.md`) remains concept-tier — one workload's four-way data extends what's known but does not settle the storage decision. Multi-component query, structural churn, attach/detach churn, mixed workload still ahead.

**Culled**: None this session. (sparsesetmap was culled forward in R-007; that's already-recorded state.)

**Retained**:
- `experiments/ecs-storage-comparison/` — experiment in flight. Six unimplemented `Storage` methods × three remaining-active backends pending; four remaining workloads pending.
- `concepts/engine/ecs-storage.md` — still load-bearing.
- `concepts/engine/entity-allocator.md` — untouched, still load-bearing.
- `concepts/engine/scheduler.md`, `render-thread.md`, `outer-tier-contract.md`, `rendering-primitives.md`, `animation-approaches.md` — engine-side concept stubs untouched.
- Five game-side concept stubs from R-004 — untouched.

**Decisions promoted**:
- D-020 — `go vet ./...` preferred over `go build ./...` for source verification (project scope; surfaced when the user redirected the build-check command mid-session). Operational rule landed in `.claude/behavior/verification.md`.
- D-021 — Functional-spec READMEs for infrastructure with illustrative content (project scope; generalized from the per-backend READMEs authored this session). Discipline: data-model invariants, algorithm shape under representative scenarios, structural behaviors that follow from the layout; avoid line-by-line code paraphrase.

**Next session focus**: Experiment-type session continuing on `experiment-ecs-storage-comparison`. Implement the multi-component query workload — second row of the workload × backend matrix. Three active backends (archetype + sparsesetslice + sparsesetgroup); sparsesetmap stays at iteration-baseline-only per R-007's cull. The workload exercises sparsesetgroup's fast-path/fallback split internally — declared owning group on `{Position, Velocity, Health}` with queries on subsets falling back to slice-style traversal — and surfaces the multi-component archetype-vs-sparse-set divergence the experiment README's *Question* section flags as the sharpest comparison axis.

**Workflow observations this session**:
- Mid-session scope expansion (per-backend READMEs across all four backends, not just sparsesetgroup) was authorized by the user and landed within the session's headroom. The expansion was structurally large but tractable because per-backend READMEs are supporting artifacts, not bound by source-code checkpoint pacing.
- Per-backend README format took two iterations to align — the first draft (technical-spec format with field bullet lists) was rejected in favor of an illustrative format (data-layout diagrams, walked-through scenarios). The accepted format is what generalized into D-021.
- Three partnership pushbacks reshaped the work this session: (1) the user proposed extracting `group.go` from `sparsesetgroup.go` once the group cluster crossed the "named abstraction" threshold (accepted, both agreed); (2) the user questioned whether `newBackend`'s sparsesetgroup case should resolve groups internally (we kept the pre-resolve-in-main approach after walking through Option A vs Option B); (3) the user redirected `go build` to `go vet` as the verification command (accepted and generalized into D-020).
- Source-code checkpoints landed at per-method granularity for the lockstep-novel parts (Spawn's two phases split with helpers as their own checkpoint) and per-file granularity for the rest. The "checkpoint sizing follows confidence" pattern from R-007 continued to apply; the user pulled `getOrCreateColumn` forward from a later checkpoint as a single mid-checkpoint request, which the framework absorbed cleanly.
- The unexpected headline (sparsesetgroup measurably *faster* than both archetype and sparsesetslice on iteration baseline, ~10–13%) inverted the plan's prediction that the three would tie. Walking through *why* — the lockstep invariant lets the iterator skip per-row sparse-side loads even when those loads always succeed in the iteration baseline — landed cleanly because the per-backend README content was already structured around exactly that data-layout reasoning.
- D-019's discipline (no measurement bias from simplifications) was load-bearing twice: (a) the slice-style fallback for non-group queries was chosen over panic-on-non-group-query specifically because production behaves the way slice-style does; (b) the unified-iterator fast-path tax (per-Next index-write loop that a specialized iterator could elide) was kept and called out in the Finding entry rather than optimized away, since a shipped general-purpose owning-group iterator would carry it.
